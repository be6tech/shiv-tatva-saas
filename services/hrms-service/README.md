# HRMS Service (Spring Boot)

## Run (local)

```bash
./mvnw spring-boot:run
```

If you don’t have Maven Wrapper yet, install Maven and run:

```bash
mvn spring-boot:run
```

## Endpoints
- `GET /health`
- `GET /employees`

